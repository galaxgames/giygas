#pragma once

namespace giygas {

    enum class AttachmentType {
        Texture,
        RenderBuffer
    };

    enum class AttachmentPurpose {
        Color,
        Depth,
        Stencil
    };

}
